# 给垃圾节点提速，寻找反代IP

> 如果你搭建了reality节点，需要注意防止反代CF，避免被人偷跑流量成为怨种。
>
> 众所周知，WS节点可以套用Cloudflare的CDN并且通过优选IP给你的垃圾节点提速

视频教程：https://bulianglin.com/archives/newcdn.html ，https://www.youtube.com/watch?v=NbruiJShUCE

### 1.域名接入cf

域名生效后需要把 **SSL/TLS**加密模式为 改成灵活

把IP解析到这个域名，如：cs.cfdhh.om

### 2.搭建WS节点

```
以v2b为例：

添加vmess节点
节点地址可以直接填ip
TLS 不支持
端口 80
传输协议：WebSocket 
编辑配置

{
  "path": "/随便",
  "headers": {
    "Host": "cs.cfdhh.om #必须填刚刚解析的域名"
  }
}
```

端口详细说明

> 如果搭建节点的端口是 80 ，那么不需要啥操作。
>
> 丨
>
> 如果搭建节点的端口不是 80 ，那么需要在cf后台→规则→Origin Rules 建新规则：
>
> 自定义筛选表达式  
>
> 字段：主机名 丨    运算符：等于 丨   值：填刚刚解析的域名 #cs.cfdhh.om
>
> 目标端口：
>
> 重写到...
>
> 搭建节点的端口
>
> 丨
>
> 上面这两种可以使用优选IP的80和443端口



> Cloudflare CDN支持的HTTP端口和HTTPS端口
>
> 支持的 HTTP 端口
>
> 80
> 8080
> 8880
> 2052
> 2082
> 2086
> 2095
>
> 支持的 HTTPS 端口
>
> 443
> 2053
> 2083
> 2087
> 2096
> 8443
>
> 如果要使用优选IP的非80和443端口，比如2052 那么搭建节点的时候端口也要用2052端口

### 3.使用

> 在此之前我先复制一个节点出来<br>80端口<br>然后将节点的地址改成刚才设置的网址 cs.cfdhh.om
> 随便设置一个名称，此时这个节点就相当于套了CF的CDN
>
> 再来测个速，可以看到速度有了不错的提升
> 但是并不稳定，尤其是电信用户，即使是优选后的IP也很难有稳定的科学上网体验，所以我们可以去优选那些反代了CF IP的IP
>
> 443端口<br>给节点套上TLS，将端口改成443，传输成安全设置为TLS , SNI 填填刚刚解析的域名 #cs.cfdhh.om<br>
> 点击确定，尝试测速，可以看到有速度说明可以正常使用

