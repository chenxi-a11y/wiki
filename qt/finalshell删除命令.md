# finalshell删除命令

在 Linux 系统中，可以使用 "rm" 命令来删除文件或目录。

如果要删除一个文件，可以使用以下命令:

```bash
rm file.txt
```

如果要删除一个目录，可以使用以下命令：

```bash
rm -r dirname
```

-r 选项用来递归删除目录下的所有文件和子目录。

如果你希望在删除之前得到确认，可以使用 -i 选项：

```bash
rm -ri dirname
```

注意在 Linux 命令行中，删除操作是不可恢复的，请谨慎使用。